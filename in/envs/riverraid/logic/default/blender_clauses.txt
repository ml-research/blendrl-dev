neural_agent(X):-same_level_enemy(P,E).
neural_agent(X):-same_level_helicopter(P,E).
neural_agent(X):-same_level_bridge(P,E).
neural_agent(X):-right_edge_river(P,R).
neural_agent(X):-left_edge_river(P,R).
neural_agent(X):-same_level_river(P,R).
logic_agent(X):-nothing_around(X).
logic_agent(X):-close_by_fuel(P,F).
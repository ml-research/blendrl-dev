neural_agent(X):-close_by_enemy_ship(P,E).
neural_agent(X):-close_by_helicopter(P,E).
neural_agent(X):-close_by_bridge(P,E).
neural_agent(X):-close_by_enemy_base(P,E).
logic_agent(X):-nothing_around(X).
logic_agent(X):-close_by_fuel(P,F).
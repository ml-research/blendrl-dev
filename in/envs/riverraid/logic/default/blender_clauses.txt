neural_agent(X):-close_by_helicopter(P,H).
neural_agent(X):-close_by_enemy_ship(P,ES).
neural_agent(X):-close_by_enemy_base(P,EB).
neural_agent(X):-close_by_bridge(P,B).
logic_agent(X):-nothing_around(X).
logic_agent(X):-close_by_fuel(P,F).
logic_agent(X):-same_level_river(P,R).
